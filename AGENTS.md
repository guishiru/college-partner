# 项目约定

## 开发流程
- 开始任务前先阅读相关 PRD、接口契约和已有测试。
- 一次只处理一个明确功能。
- 不修改无关模块。
- 完成后必须运行相关测试。

## 项目目录

项目按业务关注点组织，不按前端、后端、测试进行拆分。

```text
codex-共建/
├── AGENTS.md
├── testing.md
├── .gitignore
├── docs/
│   ├── product/       # 产品总纲
│   ├── prd/           # 各业务模块 PRD
│   ├── contracts/     # 模块之间的接口契约
│   ├── decisions/     # 重要决策及变更原因
│   └── design/        # 设计系统和页面验收材料
├── modules/           # 按业务能力拆分的功能模块
│   ├── workspace.py   # 运行时目录与状态库位置，非业务模块
│   ├── users/
│   ├── employees/
│   ├── skills/
│   ├── conversations/
│   ├── jobs/
│   ├── files/
│   ├── analysis/
│   ├── reports/
│   └── web/           # 组装层，唯一允许依赖全部模块的位置
└── tests/             # 与模块和核心流程对应的测试
```

目录规则：

- 产品文档放在 `docs/product/`，模块 PRD 放在 `docs/prd/`。
- 模块接口契约放在 `docs/contracts/`，重要技术或业务决策放在 `docs/decisions/`。
- 页面设计和验收材料放在 `docs/design/`。
- 业务代码按业务模块放在 `modules/`，不得按前端、后端、测试建立平行业务目录。
- 测试代码放在 `tests/`，并按被测模块或核心流程组织。
- 用户数据、上传文件、日志、临时文件和构建产物不得放入项目文档或源码目录。

## 模块边界与解耦

- 模块之间只能通过公开接口、明确的数据对象或约定事件通信。
- 禁止直接访问其他模块的内部数据、内部函数和内部文件。
- 每个模块负责自己的业务规则、数据和错误处理。
- 跨模块调用必须能够识别用户、会话和任务归属。
- 接口变更必须同步更新接口契约、调用方、测试和必要的 PRD。
- 不因为一个模块的内部实现变化而修改其他模块的内部实现。
- 新功能应优先放入职责最明确的现有模块；只有职责确实独立时才新增模块。
- 依赖方向由 `tests/test_module_boundaries.py` 强制。新增模块或新增跨模块依赖，必须在该测试的 `ALLOWED` 表中显式声明，并在 `docs/decisions/` 说明原因。
- `modules/users`、`modules/conversations`、`modules/employees`、`modules/jobs`、`modules/files`、`modules/reports`、`modules/skills` 是叶子模块，互不依赖。`modules/web` 是唯一的组装层。
- 工作区布局是虚拟员工的属性，由 `modules/employees` 声明，界面按当前员工渲染，不得写死成全局设定。

## 数据规则
- 用户数据必须绑定 user_id。
- 文件必须绑定 user_id、session_id、job_id。
- 不同用户之间禁止读取或覆盖数据。
- 任务失败后允许重试，但不能重复生成结果。
- 原始上传文件必须保留，不得用清洗结果覆盖原始文件。
- 数据清洗不得静默把非法值填成 0、静默删除坏行或静默改变样本量；所有变更必须可追溯并向用户展示。
- 统计口径必须以 `docs/contracts/` 中的已批准方法基线为准，不能由大模型临时编造计算规则。
- 大模型只能负责需求解析、参数补全、追问和文字解释，不能直接计算统计量或修改统计结果。
- 大模型的输出在进入确定性层之前必须校验：取值在支持范围内、引用的变量真实存在、引用的原文确实出现在用户输入中。任意一条不满足即整条作废，退回追问。
- 密钥只从环境变量读取，不得落盘、进日志或出现在异常消息中。
- 统计量必须以完整精度的数值形式输出。格式化文本可以保留，但只能是数值的派生物；任何模块都不得从展示字符串中解析数值，也不得因为取不到数值而自行重算。

## 参考项目规则
- `D:\智数统计\V517` 仅作为业务方法和知识库的参考来源，不直接作为新项目的运行目录。
- 不复制参考项目中的用户上传数据、数据库、日志、构建产物、密钥或运行时配置。
- 参考项目中的代码必须经过接口、数据安全和测试审查后，才能迁入对应业务模块。
- 统计方法迁移时必须同时迁移方法说明、参数约束、异常规则、解释规则和验证用例。

## 测试规则
- 新功能必须新增或修改测试。
- Bug 修复必须增加回归测试。
- 核心流程必须有端到端测试。
- 禁止为了通过测试而删除或跳过测试。

## Git 规则
- 每次提交只完成一个小目标。
- 提交前运行相关测试。
- 不提交密钥、用户数据、临时文件和构建产物。
- 未经确认不得修改其他模块。

## 完成标准
- PRD 验收条件全部满足。
- 正常、空数据、失败、权限场景已验证。
- 测试通过。
- 页面已截图确认。
