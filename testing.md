# 测试规则

## 测试类型
每个模块至少包含以下测试：

- 业务规则测试：数据口径是否正确。
- 接口测试：模块之间能否正确通信。
- 异常测试：空数据、错误文件、超时、权限不足等情况是否正确处理。
- 流程测试：用户能否完成完整操作。

## 执行规则

- 改代码前先查看已有测试。
- 改功能时同步补充或修改测试。
- 修复 Bug 时必须增加回归测试。
- 提交前运行相关测试。
- 核心流程必须有端到端测试。
- 禁止为了通过测试而删除或跳过测试。

## 验收规则

测试结果不能只看通过数量，还要确认：

- 失败是否影响核心流程。
- 是否有关键路径没有测试。
- 是否存在被跳过的测试。
- 是否覆盖异常和权限场景。
- 测试通过后，实际行为是否符合 PRD 验收标准。

## 持续集成

`.github/workflows/ci.yml` 在每次 push 和 PR 时跑两个任务：

- **单元测试与黄金基线**：干净环境装依赖、跑除浏览器测试外的全部用例。
- **浏览器端到端与页面截图**：启动真实服务，用 Chromium 走一遍进入工作区 → 上传 → 追问 → 确认执行 → 下载报告，并把每一步截图作为构建产物上传（失败时也上传，失败那张最有用）。

两条只在 CI 生效的硬规则：

- 依赖必须在干净环境里一次装成功。本地装好的包在 CI 上不存在，`requirements.txt` 漏了什么第一次就会红。
- 不接受任何待补基线。本地没装 `semopy` 时基线测试跳过是合理的，但 CI 是唯一能保证「11 个统计方法全部有基线」的地方，`PENDING` 非空直接失败。

端到端测试本地也能跑（`python -m pytest tests/test_end_to_end.py`），缺 Playwright 或浏览器时自动跳过，截图落在 `tests/_screenshots/`。

## 黄金基线

统计错误不会抛异常，它会返回一个看着很合理的错数。`tests/golden/` 用固定数据集把每个方法的实际数值固化下来，专门抓这类静默改动。

- 数据集 `tests/golden/baseline_survey.csv` 是夹具，不是可以随手重新生成的测试数据。它的 SHA-256 记录在 `cases.py` 里，改动会直接报错。
- 每个方法的参数在 `cases.py` 的 `CASES` 中定义，必须是确定性的；带自助法的方法要固定随机种子和抽样次数。
- 基线覆盖 `meta`、全部结果表和 `display`。`display` 不能省：线性回归的模型汇总行（R²、F 值、残差自由度、p 值）只存在于那里。
- 新增统计方法必须同时补基线；暂时补不了的要登记在 `PENDING` 并写明原因，否则测试报错。
- 比对容差为相对 1e-6，能容忍浮点末位噪声，抓得住任何真实的公式改动。

有意改动统计实现时：

```
python tests/golden/regenerate.py <method>
```

然后逐行核对 diff 再提交，并在 `docs/decisions/` 说明原因。**基线绝不允许在没看过 diff 的情况下更新**——那等于把一次回归洗进仓库。

## 数据分析专项规则

- 11 个统计方法必须分别有方法级测试，不能只测试统一调度器。
- 每个方法必须有固定样例数据和可核对的基准结果；涉及 SPSS、AMOS 或 PROCESS 口径时，必须保存对应的校验结果。
- 方法测试至少核对关键统计量、有效样本量、缺失值处理、变量角色和失败条件。
- 上传测试必须覆盖 CSV 编码、分隔符、Excel、表头空格、单元格首尾空格、空值、非法文本、重复列名和坏行。
- 任何解析失败、坏行或非法数值都不能被静默跳过或填充为 0；测试必须验证用户能看到错误和影响范围。
- 追问测试必须覆盖缺少方法、缺少变量、变量角色不完整、列名不存在和因子结构不完整。
- 文字解读测试必须验证数值来自结构化分析结果，不能凭空生成统计量或改变显著性结论。
- 核心闭环必须有端到端测试：选择数据分析师、上传文件、完成分析、查看结果、下载报告。
