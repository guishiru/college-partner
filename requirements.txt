# Data Analyst closed-loop baseline
#
#   pip install -r requirements.txt
#
# CFA 和 SEM 依赖 semopy，它的安装方式不同，见 requirements-sem.txt。
# 不装 semopy 时其余 9 个统计方法照常工作，这两个方法会返回明确的依赖缺失错误。
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
statsmodels>=0.14.0
openpyxl>=3.1.0
xlrd>=2.0.1
factor_analyzer>=0.5.1

# Web application and report generation
Flask>=3.0.0
python-docx>=1.1.0
